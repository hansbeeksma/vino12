<svg viewBox="0 0 80 280" xmlns="http://www.w3.org/2000/svg">
  <!-- Rhone: Slanted shoulders, wider than Bordeaux, embossed coat of arms area -->
  <!-- Cork/capsule -->
  <rect x="34" y="0" width="12" height="8" fill="currentColor"/>
  <!-- Neck -->
  <rect x="32" y="8" width="16" height="50" fill="currentColor"/>
  <!-- Collar ring -->
  <rect x="28" y="58" width="24" height="6" fill="currentColor"/>
  <!-- Shoulders (medium slope, wider than Bordeaux) -->
  <path d="M28 64 Q20 85 10 115 L70 115 Q60 85 52 64 Z" fill="currentColor"/>
  <!-- Embossed area (coat of arms indent) -->
  <rect x="10" y="115" width="60" height="30" fill="currentColor"/>
  <!-- Body (slightly wider, gentle taper) -->
  <path d="M10 145 L12 248 L68 248 L70 145 Z" fill="currentColor"/>
  <!-- Base -->
  <rect x="8" y="248" width="64" height="8" fill="currentColor"/>
  <rect x="10" y="256" width="60" height="4" fill="currentColor"/>
</svg>
